#include <gtest/gtest.h>

TEST(ClientTime_Robustesse, NoRobustnessScenariosYet)
{
  GTEST_SKIP()
      << "No documented cases of errors/invalid input for these utilities. "
         "Add here if the API changes (e.g., empty pool, invalid formats, "
         "etc.).";
}
